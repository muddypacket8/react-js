import React from 'react';

const Users = (props) => {
    return (
        //What I want to display
        <div>
            <p>This is employee {props.name}</p>
        </div>

    );

}
export default Users;