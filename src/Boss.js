import React from 'react';

const Boss = (prop) => {
    return (
        //What I want to display
        <div>
            <p>This is boss {prop.name}</p>
        </div>

    );

}
export default Boss;